{
  "name": "@priyx/sandbox-storage",
  "version": "2.0.0",
  "private": true,
  "description": "Ultra-fast file storage service for Magical sandbox workspaces (Go + Redis)",
  "scripts": {
    "build": "cd go && go build -o ../sandbox-storage.exe .",
    "start": "./sandbox-storage.exe",
    "test": "cd go && go test ./..."
  }
}
